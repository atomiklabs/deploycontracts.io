export function Step1Icon(props: any) {
  const { step, ...other } = props
  return (
    <div {...other}>
      <img src={step} alt='step icon' />
    </div>
  )
}

export function Step2Icon(props: any) {
  const { step, ...other } = props
  return (
    <div {...other}>
      <img src={step} alt='step icon' />
    </div>
  )
}

export function Step3Icon(props: any) {
  const { step, ...other } = props
  return (
    <div {...other}>
      <img src={step} alt='step icon' />
    </div>
  )
}

export function Step4Icon(props: any) {
  const { step, ...other } = props
  return (
    <div {...other}>
      <img src={step} alt='step icon' />
    </div>
  )
}
